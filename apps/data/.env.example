# Data pipeline configuration (issues #34/#35) — copy to .env, never commit .env.
# Database contract: docs/DATABASE_ARCHITECTURE.md §6.4

# Direct (unpooled) Neon connection string — migrations and bulk ingest only.
# (The pooled DATABASE_URL is the Next.js app's; the pipeline never needs it.)
DATABASE_URL_UNPOOLED=postgresql://USER:PASSWORD@ep-xxx.REGION.aws.neon.tech/neondb?sslmode=require

# Extraction + embedding (see apps/data/EXTRACTION_MANUAL.md §1).
# Keys are PER PERSON — every club member uses their own; never share or commit.
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
# provider:model for the extraction engine (anthropic:* | lmstudio:* | ollama:*)
EXTRACTOR=anthropic:claude-sonnet-5
# absolute path to the raw scraped corpus (outside the repo)
RAW_ROOT=
# used only by run_catalog.ps1 as its output root; the python CLIs take --out flags
FACTS_OUT=

# Embeddings are produced by pipeline/embed_rows.py via OpenRouter:
# openai/text-embedding-3-small at dimensions=768, provider-pinned to OpenAI
# (team decision 2026-07-25) — the SAME model must be used at query time.

# Chunking (issue #35)
CHUNK_SIZE_TOKENS=800
CHUNK_OVERLAP_PCT=15

# Local Chroma Vector Database Configuration
CHROMA_DB_PATH=apps/data/chroma_db
CHROMA_COLLECTION_NAME=dallas_college_kb
