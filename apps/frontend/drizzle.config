/**
 * Drizzle configuration placeholder.
 *
 * This project uses SQLAlchemy + Alembic as the authoritative
 * schema and migration system.
 *
 * Drizzle is used only on the frontend consumption side.
 * This file exists as a future extension point if Drizzle
 * tooling is ever introduced.
 */

export const drizzleConfig = {
  schemaDir: "./lib/db/schema",
};

export default drizzleConfig;
